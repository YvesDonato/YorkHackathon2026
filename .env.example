# Frontend public URL allowed by backend CORS.
FRONTEND_ORIGIN=https://app.example.com

# URL the frontend uses to call FastAPI (embedded in Next.js public env).
NEXT_PUBLIC_FASTAPI_BASE_URL=https://api.example.com
